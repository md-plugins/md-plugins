{
  "name": "@md-plugins/mono-repo",
  "version": "2.0.2",
  "private": true,
  "description": "Markdown-it, Vite, and Quasar documentation tooling for Vue/Vite projects.",
  "keywords": [
    "markdown",
    "markdown-it",
    "quasar",
    "quasarframework",
    "vue"
  ],
  "license": "MIT",
  "author": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm build:packages && pnpm build:docs:ssg",
    "build:docs:ssg": "pnpm --dir packages/docs build:ssg",
    "build:packages": "pnpm --filter './packages/*' --filter '!mdplugins_docs' --stream build",
    "dev": "vitest",
    "check-types": "pnpm check-types:packages && pnpm build:packages && pnpm --filter mdplugins_docs check-types",
    "check-types:packages": "tsc -p tsconfig.json --noEmit --skipLibCheck",
    "deep": "ncu --configFilePath . --packageFile '{package.json,packages/*/package.json}'",
    "clean": "pnpm -r --stream clean",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "check:all": "pnpm verify",
    "verify": "pnpm verify:fast && pnpm verify:build",
    "verify:fast": "pnpm format:check && pnpm lint && pnpm test",
    "verify:build": "pnpm build && pnpm check:qpress-runtime && pnpm check-types:packages && pnpm --filter mdplugins_docs check-types",
    "check:qpress-runtime": "node -e \"import('./packages/qPress/dist/index.js')\"",
    "ci:publish": "pnpm -r publish --access public --publish-branch dev",
    "ci:publish:latest": "pnpm -r publish --access public --publish-branch dev --tag latest",
    "ci:publish:beta": "pnpm -r publish --access public --publish-branch dev --tag beta",
    "ci:publish:rc": "pnpm -r publish --access public --publish-branch dev --tag rc",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@vitest/coverage-istanbul": "^4.1.10",
    "npm-check-updates": "^23.0.1",
    "obuild": "^0.4.38",
    "oxfmt": "^0.62.0",
    "oxlint": "^1.77.0",
    "typescript": "~6.0.3",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=22.13",
    "pnpm": ">=11.5.0"
  },
  "packageManager": "pnpm@11.20.0",
  "access": "private"
}
